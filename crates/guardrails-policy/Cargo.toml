[package]
name = "guardrails-policy"
version = "0.1.0"
description = "Deny-by-default capability policy kernel for GuardRails IDE"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"

[lints]
workspace = true
